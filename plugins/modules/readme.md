*from this directory*

Unit Tests
----------

```(ansible) $ pytest [--cov=. --flake8 --cov-report=html]```

Type Check
----------

```(ansible) $ mypy --module nm```

Check DocStrings
----------------

```(ansible) $ ansible-doc --type=module nm```
